layers can be thought of as filters for data
layers extract representations out of data fed into them

most deep learning conists of chaining together simple layers that will implment form of progressive data distillation

Dense/fully connected neural networks -> densely connected neural layers (every neuron connects to every neuron in previous layer)

10-way softmax classicfication yer -> return an array of 10 prob scores summing to 1

optimizer - mechanizm which model will update itself based on training data it sees

loss function - how model should measure performance on training data

[[Data Represenations]]

[[Tensor Operations]]
[[Gradeient Optimzation]]
