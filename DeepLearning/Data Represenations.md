tesnors are the fundamental basic data structure (numpy arrays)

container for data. (We talked a bit about this in lin alg)

rank-0 tensors:
- scalar value

rank-1 tensors:
- vector

rank2 tensors:
- matrix

tensor def by three key attr:
- rank
- Shape -> (row,column) vectors are (row) and scalars ()
- Data type -> what is the primitive essenitally

deep learning models split data into batches the first axis (axis 0) is called the batch axis or batch dimension

